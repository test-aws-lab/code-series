version: 0.2

phases:
  build:
    commands:
      - aws deploy push --region ap-northeast-1 --application-name test-application --s3-location s3://code-build-test-aws-cloud/sample.zip --source src
artifacts:
  files:
    - '**/*'
  base-directory: src
  name: sample.zip